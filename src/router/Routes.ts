enum Routes {
  Home = '/',
  Login = '/login',
  Join = '/join',
  SongLibrary = '/songs',
  Song = '/song/:songId',
  Session = '/session',
  SessionList = '/sessionList',
  Info = '/info',
}

export default Routes;

enum Routes {
  Home = '/',
  Login = '/login',
  Join = '/join',
  SongLibrary = '/songs',
  SongCreate = '/song/new',
  Song = '/song/:songId',
  Session = '/session',
  SessionList = '/sessionList',
  Info = '/info',
}

export default Routes;
